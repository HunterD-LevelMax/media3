<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ExoPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Для Android 6.0+ (API 23+) -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/blue</item>

        <!-- Для Material 3 -->
        <item name="colorSurface">@color/blue</item>
        <item name="colorOnSurface">@color/blue</item>
    </style>

    <style name="Theme.ExoPlayer" parent="Base.Theme.ExoPlayer" />
</resources>